rdimg7.c	(except atn images)

scaler = (0.000001 * mh7->calibration_factor * sh7->scale_factor * sh7->frame_duration) / sh7->decay_corr_fctr;

for (i = 0; i < len; i++)
   matrix[i] = scaler * simg[i];

